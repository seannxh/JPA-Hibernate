create table course(

    id bigint not null,
    name varchar(255) not null,
    author varchar(255) not null,
    primary key (id)
);
--
--JDBC a lot of sql codes and java
--Spring JDBC same amount of sql but not as much as java